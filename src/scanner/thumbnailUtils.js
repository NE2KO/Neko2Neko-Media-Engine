export function hasEmbeddedCover(filePath) {
  return false;
}

export async function extractEmbeddedThumbnail(filePath, outputPath) {
  return null;
}

export async function extractFrameThumbnail(filePath, outputPath, opts = {}) {
  return null;
}

export async function generateImageThumbnail(filePath, outputPath, opts = {}) {
  return null;
}

export async function generateAudioPlaceholder(outputPath, title = '', artist = '') {
  return null;
}
